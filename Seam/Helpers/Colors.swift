import SwiftUI

extension Color {
    // Vintage terracotta — replaces system teal throughout the app
    static let terracotta = Color(red: 0.722, green: 0.361, blue: 0.220)

    // Warm cream for card surfaces (replaces pure white)
    static let warmCard = Color(red: 1.0, green: 0.988, blue: 0.969)

    // Warm brown-tinted shadow
    static let warmShadow = Color(red: 0.282, green: 0.149, blue: 0.067)

    // Antique deep teal — aged copper patina, pairs with terracotta
    static let antiqueTeal = Color(red: 0.176, green: 0.357, blue: 0.341)

    // Paper beige — matches canvas drawing surface (keep in sync with PaperTexture.baseColor)
    static let paperBeige = Color(red: 0.900, green: 0.850, blue: 0.752)
}
